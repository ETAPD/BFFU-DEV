# Spanish Civil War balance handicap. The value lives in SPA_scw_balance_var so
# it can decay: it starts at -0.10 and, from 1 January 1937, gains 0.01 each
# month until it reaches zero ten months later. Held by the Nationalist side and
# by any country whose volunteers fight for them.
SPA_scw_balance_modifier = {
	enable = { always = yes }

	icon = GFX_idea_SPR_nationalist_remnants

	remove_trigger = {
		has_global_flag = scw_over
	}

	army_attack_factor = SPA_scw_balance_var
	army_defence_factor = SPA_scw_balance_var
}
