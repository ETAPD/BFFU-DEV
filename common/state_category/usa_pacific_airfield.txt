state_categories = {
	# Forward Pacific airfield islands (Taiwan, Okinawa, Iwo Jima, Marcus Island).
	# The "Prepare Invasion Regions" focus swaps the islands into this category
	# (raising the air_base cap from their normal 2-10 up to 12) and sets the air
	# base to 12. local_building_slots = air_base cap, so once the focus places the
	# level-12 field every shared slot is full and no more air bases can be built.
	# Before the focus the islands keep their normal low-cap categories, so the
	# level-12 staging fields can only ever come from the focus.
	usa_pacific_airfield = {
		local_building_slots = 12
		local_supplies = 0.1
		buildings_max_level = {
			naval_base = 10
			air_base = 12
		}
		color = { 64 110 190 }
	}
}
