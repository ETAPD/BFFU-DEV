GER_french_repayments_category = {
	GER_french_repayments_increase = {
		icon = GFX_decision_generic_construction

		allowed = { tag = GER }

		targets = { VIC }
		target_trigger = {
			FROM = { exists = yes }
		}

		cost = 50

		available = {
			custom_trigger_tooltip = {
				tooltip = GER_french_repayments_available_tt1
				VIC = {
					check_variable = { VIC_go_mic_to_overlord_factor < 0.25 }
				}
			}
		}

		complete_effect = {
			VIC = {
				add_to_variable = { var = VIC_go_mic_to_overlord_factor value = 0.05 }
			}
			custom_effect_tooltip = GER_french_repayments_complete_tt1
		}
	}

	GER_french_repayments_decrease = {
		icon = GFX_decision_generic_construction

		allowed = { tag = GER }

		targets = { VIC }
		target_trigger = {
			FROM = { exists = yes }
		}

		cost = 50

		available = {
			custom_trigger_tooltip = {
				tooltip = GER_french_repayments_available_tt2
				VIC = {
					check_variable = { VIC_go_mic_to_overlord_factor > 0 }
				}
			}
		}

		complete_effect = {
			VIC = {
				subtract_from_variable = { var = VIC_go_mic_to_overlord_factor value = 0.05 }
			}
			custom_effect_tooltip = GER_french_repayments_complete_tt2
		}
	}
}
