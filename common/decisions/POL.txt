POL_defensive_plans = {
	POL_defend_in_east_state = {

		icon = hol_draw_up_staff_plans

		state_target = yes

		available = {
			NOT = {any_state = { has_dynamic_modifier = {modifier = POL_soviet_campaign scope = POL } } }
			has_war_with = GER
			is_government_in_exile = yes
			SOV = {has_war_with = GER}
		}
		target_trigger = {
			FROM = {
			OR = {
			is_core_of = SOV
			is_core_of = POL			
			}
				OR = {
					AND = {
						is_controlled_by = SOV
						any_neighbor_state = {
							OR = {
								is_controlled_by = ITA	
								is_controlled_by = GER
								is_controlled_by = HUN	
								is_controlled_by = ROM
								is_controlled_by = BUL
								is_controlled_by = D04
								is_controlled_by = YUG	
								is_controlled_by = SPR									
							}

						}
					}
					AND = {
						OR = {
								is_controlled_by = ITA	
								is_controlled_by = GER
								is_controlled_by = HUN	
								is_controlled_by = ROM
								is_controlled_by = BUL
								is_controlled_by = D04
								is_controlled_by = YUG	
								is_controlled_by = SPR									
						}
						any_neighbor_state = {
							is_controlled_by = SOV
						}
					}
				}
			}
		}
		visible = {
		has_completed_focus = POL_local_eastern_plans
		}

		fire_only_once = no
		cost = 30

		days_re_enable = 28
		on_map_mode = map_only
		complete_effect = {
			FROM = { 
				add_dynamic_modifier = {
					modifier = POL_soviet_campaign
					days = 14
					scope = POL
				}
			}
		}

		ai_will_do = {
			base = 0
		}
	}
	POL_defend_in_west_state = {

		icon = hol_draw_up_staff_plans

		state_target = yes

		available = {
			NOT = {any_state = { has_dynamic_modifier = {modifier = POL_african_campaign scope = POL } } }
			has_war_with = GER
			is_government_in_exile = yes
		}
		target_trigger = {
			FROM = {
				is_on_continent = africa
				OR = {
					AND = {
						OR = {
							is_controlled_by = POL						
							is_controlled_by = ENG	
							is_controlled_by = FRA
							is_controlled_by = SAF
							is_controlled_by = BEL
							is_controlled_by = COG				
						}
						any_neighbor_state = {
							OR = {
								is_controlled_by = ITA	
								is_controlled_by = GER
								is_controlled_by = HUN	
								is_controlled_by = ROM
								is_controlled_by = BUL
								is_controlled_by = D04
								is_controlled_by = YUG	
								is_controlled_by = SPR									
							}
						}
					}
					AND = {
							OR = {
								is_controlled_by = ITA	
								is_controlled_by = GER
								is_controlled_by = HUN	
								is_controlled_by = ROM
								is_controlled_by = BUL
								is_controlled_by = D04
								is_controlled_by = YUG	
								is_controlled_by = SPR									
							}
						any_neighbor_state = {
						OR = {
							is_controlled_by = POL						
							is_controlled_by = ENG	
							is_controlled_by = FRA
							is_controlled_by = SAF
							is_controlled_by = BEL
							is_controlled_by = COG				
						}
						}
					}
				}
			}
		}
		visible = {
			has_completed_focus = POL_local_western_plans
		}

		fire_only_once = no
		cost = 30

		days_re_enable = 28
		on_map_mode = map_only
		complete_effect = {
			FROM = { 
				add_dynamic_modifier = {
					modifier = POL_african_campaign
					days = 14
					scope = POL
				}
			}
		}

		ai_will_do = {
			base = 10
		}
	}
}








POL_colonies_development  = { 
    POL_return_to_warsaw = {
        icon = GFX_decision_SWI_no_elected_president
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
		}
		cost = 0
		 
        available = { 
			10 = {is_controlled_by = POL}
			has_completed_focus = POL_prepare_for_the_inevitable
		}
        fire_only_once = yes
		complete_effect = {
			set_capital =  { state = 10 }
		}

    }
    POL_develop_murzynowo_naval_base = {
        icon = GFX_decision_generic_orginization_of_african_unity
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
			has_capitulated = yes
		}
		cost = 0
		 
        available = { 
		298 = {is_controlled_by = POL}	
		}
        fire_only_once = yes
		complete_effect = {
			set_capital =  { state = 298 }
			298 = {
				add_building_construction = {
					type = naval_base
					level = 5
					province = 7959
					instant_build = yes
				}
			}
		}

    }
    POL_spend_token_infrastructure = {
        icon = generic_construction
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
			has_capitulated = yes
		}
		days_remove = 35
		cost = 0
		
        available = { 
		has_legitimacy > 35
		check_variable = { POL_reconstruction_tokens > 0 } 
		any_state = {
        is_owned_and_controlled_by = POL
						free_building_slots = {
						building = infrastructure
						size > 2
					}
    }
		}
        fire_only_once = no
		complete_effect = {
				subtract_from_variable = { POL_reconstruction_tokens = 1 }

			}
		remove_effect = {
			#subtract_from_variable = { POL_reconstruction_tokens = 1 }
			
			random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_owned_and_controlled_by = POL
					ROOT = { has_full_control_of_state = PREV }
					OR = {
						infrastructure < 8
						NOT = { infrastructure > 7 }
					}
				}
				
				add_building_construction = { type = infrastructure level = 2 instant_build = yes }

			}
				custom_effect_tooltip = POL_spend_token_infrastructure_decision_tooltip
				custom_effect_tooltip = POL_reconstruction_token_count_tooltip 
		}

    }

    # 2. Program Przemysłowy (Off-map)
    POL_spend_token_industry = {
        icon = GFX_decision_generic_factory
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
			has_capitulated = yes
		}
		days_remove = 35
		cost = 0
		
        available = { 
		check_variable = { POL_reconstruction_tokens > 0 } 
		has_legitimacy > 40	
		has_war = yes
		has_capitulated = yes	

		
		}
        fire_only_once = no
        		complete_effect = {
				subtract_from_variable = { POL_reconstruction_tokens = 1 }

			}
        remove_effect  = {
            #subtract_from_variable = { POL_reconstruction_tokens = 1 }
            random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_controlled_by = ROOT
				}
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			custom_effect_tooltip = POL_spend_token_infrastructure_decision_tooltip
			custom_effect_tooltip = POL_reconstruction_token_count_tooltip 
        }
    }

    # 3. Zbrojenia Wojskowe (Off-map)
    POL_spend_token_military = {
        icon = GFX_decision_generic_fortification
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
			has_capitulated = yes
		}
		days_remove = 35
		cost = 0
		
        available = { 
		
		check_variable = { POL_reconstruction_tokens > 0 }
		has_legitimacy > 50
		has_war = yes
		has_capitulated = yes	

		}
        fire_only_once = no
        		complete_effect = {
				subtract_from_variable = { POL_reconstruction_tokens = 1 }

			}
        remove_effect  = {
            #subtract_from_variable = { POL_reconstruction_tokens = 1 }
            random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_controlled_by = ROOT
				}
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
			custom_effect_tooltip = POL_spend_token_infrastructure_decision_tooltip
			custom_effect_tooltip = POL_reconstruction_token_count_tooltip 
        }
    }

    # 4. Odbudowa Floty (Off-map)
    POL_spend_token_navy = {
        icon = GFX_decision_generic_merge_plant_ship
		allowed = {
			tag = POL
		}
		visible = {	
		has_war = yes
		has_capitulated = yes
		}
		days_remove = 35
		cost = 0
		
        available = { 
		check_variable = { POL_reconstruction_tokens > 0 } 
		has_legitimacy > 40
		has_war = yes
		has_capitulated = yes		
		}
        fire_only_once = no
        		complete_effect = {
				subtract_from_variable = { POL_reconstruction_tokens = 1 }

			}
        remove_effect  = {
            #subtract_from_variable = { POL_reconstruction_tokens = 1 }
            random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_controlled_by = ROOT
					is_coastal = yes
				}
				add_extra_state_shared_building_slots =1
				add_building_construction = {
					type = dockyard
					level = 1
					instant_build = yes
				}
			}
			
			custom_effect_tooltip = POL_spend_token_infrastructure_decision_tooltip
			custom_effect_tooltip = POL_reconstruction_token_count_tooltip 
        }

    }


	# 6. Decyzja dla Stali
	POL_spawn_steel_refinery = {
		icon = GFX_decision_steel
		
		allowed = { tag = POL }
		
		visible = {
			has_war = yes
			has_capitulated = yes
		}
		days_remove = 100
		cost = 0
		available = { 
		check_variable = { POL_steel_refinery_tokens > 0 } 
		has_legitimacy > 25		
		has_war = yes
		has_capitulated = yes
		any_state = {
			is_owned_and_controlled_by = POL
						free_building_slots = {
						building = industrial_complex
						size > 2
						}
		}
		
		
		}
		complete_effect = {
				subtract_from_variable = { POL_steel_refinery_tokens = 1 }

			}
		remove_effect  = {
			#subtract_from_variable = { POL_steel_refinery_tokens = 1 }
			
			random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_owned_and_controlled_by = POL
						free_building_slots = {
						building = industrial_complex
						size > 2
						}
				}
				add_building_construction = { type = steel_refinery level = 1 instant_build = yes }

			}
			custom_effect_tooltip = POL_steel_refinery_spend_token_tooltip	
			custom_effect_tooltip = POL_steel_refinery_token_count_display_tooltip
		}
	}

	# 7. Decyzja dla Rafinerii Syntetycznej
	POL_spawn_synthetic_refinery = {
		icon = GFX_decision_rubber
		
		allowed = {
			tag = POL
		}
		visible = {
			has_war = yes
			has_capitulated = yes
			has_country_flag = POL_eligible_for_refineries_on_cap
		}
		days_remove = 100
		cost = 0
		
		available = {
		check_variable = { POL_synthetic_refinery_tokens > 0 } 
		has_legitimacy > 25	
		has_war = yes
		has_capitulated = yes		 
				any_state = {
			is_owned_and_controlled_by = POL
						free_building_slots = {
						building = industrial_complex
						size > 2
						}
		}
		}
		complete_effect = {
				subtract_from_variable = { POL_synthetic_refinery_tokens = 1 }

			}
		remove_effect  = {
			# Odjęcie tokena
			#subtract_from_variable = { POL_synthetic_refinery_tokens = 1 }
				random_owned_state = {
				prioritize = { 689 318 }
				limit = {
					is_owned_and_controlled_by = POL
						free_building_slots = {
						building = industrial_complex
						size > 2
						}
				}
				add_building_construction = { type = synthetic_rubber_refinery level =1 instant_build = yes }

			}
			custom_effect_tooltip = POL_synthethic_refinery_spend_token_tooltip
			custom_effect_tooltip = POL_synthethic_refinery_token_count_display_tooltip
				
		
			
		}
	}
	
	
		POL_assemble_air_squadron_with_token = {

		icon = generic_air

		allowed = {
			tag = POL
		}

		available = {
			has_war = yes
			has_capitulated = yes
			has_legitimacy > 50
			check_variable = { POL_army_recruitment_tokens > 0 }
		}

		days_remove = 70

		cost = 0

		fire_only_once = no

		visible = {
			tag = POL
			has_war = yes
			has_capitulated = yes
		}

		modifier = {
		}

		complete_effect = {
				subtract_from_variable = { POL_army_recruitment_tokens = 1 }

			}

		remove_effect = {
		
		#subtract_from_variable = { POL_army_recruitment_tokens = 1 }
			add_equipment_to_stockpile = {
			    type = fighter_equipment
			    amount = 40
			    producer = ENG
			}
			custom_effect_tooltip = POL_spend_token_army_decision_tooltip
			custom_effect_tooltip = POL_army_recruitment_token_count_display_tooltip	
		}

	}
	
		POL_request_infantry_equipment_with_token = {

		icon = GFX_decision_SWI_guisan_speech
		allowed = {
			tag = POL
		}

		available = {
			has_war = yes
			has_capitulated = yes
			has_legitimacy > 50
			check_variable = { POL_army_recruitment_tokens > 0 }
		}

		days_remove = 45

		cost = 0

		fire_only_once = no

		visible = {
			tag = POL
			has_war = yes
			has_capitulated = yes
		}
		modifier = {
		}

		complete_effect = {
				subtract_from_variable = { POL_army_recruitment_tokens= 1 }

			}

		remove_effect = {

			
		#subtract_from_variable = { POL_army_recruitment_tokens = 1 }
			add_equipment_to_stockpile = {
			    type = infantry_equipment
			    amount = 1000
			    producer = POL
			}
			add_equipment_to_stockpile = {
			    type = squad_equipment
			    amount = 200
			    producer = POL
			}
			add_equipment_to_stockpile = {
			    type = support_equipment
			    amount = 150
			    producer = POL
			}
			custom_effect_tooltip = POL_spend_token_army_decision_tooltip
			custom_effect_tooltip = POL_army_recruitment_token_count_display_tooltip	
		}
	}
	
	POL_request_factory_access_decision = {
		icon = GFX_decision_generic_industry

		allowed = {

		}
		visible = {
			tag = POL
			has_war = yes
			has_capitulated = yes
		}
		available = {
			original_tag = POL 
			has_capitulated = yes
			has_legitimacy > 50		
		}

		targets = { ENG  }

		target_trigger = {
			FROM = { 
				OR = {
					AND = { 
						is_in_faction_with = ROOT
						has_capitulated = no
						NOT = { original_tag = USA } 
					}
				}
			}
		}

		cost = 70

		fire_only_once = no

		days_remove = 70

		complete_effect = {
			set_country_flag = POL_IOG_currently_undertaking_action_flag
		}

		remove_effect = {
			clr_country_flag = POL_IOG_currently_undertaking_action_flag

			add_offsite_building = {
				type = industrial_complex
				level = 1
			}
		}
	}

	POL_declare_war_on_finland	= {
		icon = GFX_decision_generic_industry
		
		
		
		allowed = {

		}
		visible = {
			tag = POL
			has_completed_focus = POL_polish_peoples_army
			SOV = {has_war_with = FIN}
			NOT = { POL = {has_war_with = FIN}}	
		}
		available = {
			tag = POL
			has_completed_focus = POL_polish_peoples_army
			SOV = {has_war_with = FIN}
			NOT = { POL = {has_war_with = FIN}}	
		}

        fire_only_once = yes
		complete_effect = {
			POL = {
				declare_war_on = {
					target = FIN
					type = annex_everything
				}
			}
		}
	}


}

POL_invite_foreign_motor_decisions = {
	POL_invite_ford = {

		icon = infiltrate_state

		available = {
			USA = {
				has_opinion = {
					target = POL
					value > 25
				}
			}
		}

		visible = {
			has_completed_focus = POL_attract_foreign_motors
			NOT = {
				has_country_flag = invited_motor_co
			}
		}

		cost = 0
		fire_only_once = yes
		ai_will_do = {
			factor = 2
		}
		complete_effect = {
			if = {
				limit = {
					has_dlc = "Arms Against Tyranny"
				}
				custom_effect_tooltip = available_mio_tt
				show_mio_tooltip = USA_ford_motor_company_organization
			}
			else = {
				show_ideas_tooltip = POL_ford
			}
			set_country_flag = invited_motor_co
			set_country_flag = POL_ford_motors
		}
	}
	POL_invite_vauxhall = {

		icon = infiltrate_state

		available = {
			ENG = {
				has_opinion = {
					target = POL
					value > 25
				}
			}
		}

		visible = {
			has_completed_focus = POL_attract_foreign_motors
			NOT = {
				has_country_flag = invited_motor_co
			}
		}

		cost = 0
		fire_only_once = yes
		ai_will_do = {
			factor = 2
		}
		complete_effect = {
			if = {
				limit = {
					has_dlc = "Arms Against Tyranny"
				}
				custom_effect_tooltip = available_mio_tt
				show_mio_tooltip = ENG_vauxhall_automotive_organization
			}
			else = {
				show_ideas_tooltip = POL_vauxhall
			}
			set_country_flag = invited_motor_co
			set_country_flag = POL_vauxhall_motors
		}
	}
	POL_invite_somua = {

		icon = infiltrate_state

		available = {
			FRA = {
				has_opinion = {
					target = POL
					value > 25
				}
			}
		}

		visible = {
			has_completed_focus = POL_attract_foreign_motors
			NOT = {
				has_country_flag = invited_motor_co
			}
		}

		cost = 0
		fire_only_once = yes
		ai_will_do = {
			factor = 2
		}
		complete_effect = {
			if = {
				limit = {
					has_dlc = "Arms Against Tyranny"
				}
				custom_effect_tooltip = available_mio_tt
				show_mio_tooltip = FRA_somua_organization
			}
			else = {
				show_ideas_tooltip = POL_somua
			}
			set_country_flag = invited_motor_co
			set_country_flag = POL_somua_motors
		}
	}
}


POL_reopen_the_maritime_and_colonial_league = {

	POL_purchase_liberia = {

		icon = decision_generic_nationalism

		allowed = {
		}

		available = {
			298 = {
				owner = {
					is_in_faction_with = ROOT
				}
			}
			700 = {
				owner = {
					is_in_faction_with = ROOT
				}
			}			
		}
		
		visible = {
			NOT = {
				owns_state = 298
				owns_state = 700				
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = 298
			}
		}

		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = no
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
				700 = { 
					transfer_state_to = POL 
					set_compliance = 60 
				}
				298 = { 
					transfer_state_to = POL 
					set_compliance = 60 
				}
				add_victory_points = {
					  province = 7959
					  value = 5
				}
				298 = {
					set_state_name = "Nowy Gdańsk"
				}
					set_province_name = { id = 7959 name = "Murzynowo" }
				
				
				700 = {
					set_state_name = "Pomorze Afrykańskie"
				}
				set_province_name = { id = 2038 name = "Przylądek Czarnej Nadziei" }
				add_victory_points = {
				  province = 2038
				  value = 2
				}
		}

		ai_will_do = {
			factor = 10
		}

	}
	POL_purchase_gambia = {

		icon = decision_generic_nationalism

		allowed = {
		}

		available = {
			701 = {
				owner = {
					is_in_faction_with = ROOT
				}
			}		
		}
		
		visible = {
			NOT = {
				owns_state = 701				
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = 701
			}
		}

		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = no
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
				701 = { 
					transfer_state_to = POL 
					set_compliance = 60 
					set_state_category = rural
				}
				set_province_name = { id = 4989 name = "Inżynierowo" }

		}

		ai_will_do = {
			factor = 10
		}

	}
	POL_purchase_jamaica = {

		icon = decision_generic_nationalism

		allowed = {
		}

		available = {
			689 = {
				owner = {
					is_in_faction_with = POL
				}
			}		
		}
		
		visible = {
			NOT = {
				owns_state = 689				
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = 689
			}
		}

		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = no
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
				689 = { 
					transfer_state_to = POL 
					set_compliance = 60 
				}

				689 = {
					set_state_name = "Japolska"
				}
				set_province_name = { id = 12304 name = "Port Polska" }

			add_resource = {
				type = oil
				amount = 50
				state = 689
			}

			add_resource = {
				type = tungsten
				amount = 100
				state = 689
			}

			add_resource = {
				type = steel
				amount = 200
				state = 689
			}

			add_resource = {
				type = chromium
				amount = 100
				state = 689
			}
		}

		ai_will_do = {
			factor = 10
		}

	}

	POL_purchase_haiti = {

		icon = decision_generic_nationalism

		allowed = {
		}

		available = {	
			has_capitulated = yes		
		}
		
		visible = {
			NOT = {
				owns_state = 318				
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = 318
			}
		}

		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = no
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
				hidden_effect = {
					BAS = {
						add_state_core = 318
					}
				}
				318 = { 
					transfer_state_to = POL 
					set_compliance = 60 
					# add_manpower = 1100000
					set_state_name = "Praga Zachód"
					add_building_construction = {
						type = naval_base
						level = 3
						instant_build = yes
						province = 4641
					}
				}

			add_victory_points = {
			  province = 4641
			  value = 2
			}
			add_resource = {
				type = aluminium
				amount = 150
				state = 318
			}
			random_state = {
				prioritize = { 318 }
				add_building_construction = {
					type = infrastructure
					level = 4
					instant_build = yes
				}
			}			
			
			
			random_owned_controlled_state = {
				prioritize = { 318 }
				add_extra_state_shared_building_slots = 2
			}
					if = {
						limit = { NOT = { has_variable = POL_reconstruction_tokens } }
						set_variable = { POL_reconstruction_tokens = 0 }
					}
					add_to_variable = {
						var = POL_reconstruction_tokens
						value = 1
						tooltip = POL_reconstruction_token_gain_1_tt
					}
					custom_effect_tooltip = POL_reconstruction_token_usage_tooltip	
		}

		ai_will_do = {
			factor = 10
		}

	}
	
	POL_attract_poles_to_japolska = {

		icon = decision_generic_nationalism

		allowed = {
		}

		available = {
			has_capitulated = yes		
			any_owned_state = {
				is_coastal = yes 
				state = 689
				is_controlled_by = ROOT
			}
		}
		
		visible = {
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = 689
			}
		}

		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = yes
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
			if = {
				limit = {
					owns_state = 689
				}
				85 = { add_manpower = -35000 }
				random_owned_controlled_state = {
					limit = {
						is_coastal = yes
					}
					prioritize = { 689}
					add_manpower = 35000
				}
			}
			random_owned_controlled_state = {
				prioritize = { 689 }
				limit = {
					free_building_slots = {
						building = dockyard
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 4
				add_building_construction = {
					type = dockyard
					level = 2
					instant_build = yes
				}
			}
			
					if = {
						limit = { NOT = { has_variable = POL_reconstruction_tokens } }
						set_variable = { POL_reconstruction_tokens = 0 }
					}
					add_to_variable = {
						var = POL_reconstruction_tokens
						value = 1
						tooltip = POL_reconstruction_token_gain_1_tt
					}
					custom_effect_tooltip = POL_reconstruction_token_usage_tooltip	
		}

		ai_will_do = {
			factor = 10
		}

	}	 
	
	# Purchasing Cameroon
	POL_purchase_cheap_colony = {

		icon = decision_generic_nationalism
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				OR = {
					state = 693
					state = 690					
				}
			}
		}

		available = {
			#has_capitulated = yes
			FROM = {
				is_capital = no 
				owner = {
					is_in_faction_with = ROOT
				}					
			}
		
		}
		
		visible = {
			NOT = {
				owns_state = FROM
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				state = FROM
			}
		}


		on_map_mode = map_and_decisions_view

		custom_cost_trigger = {
			num_of_civilian_factories_available_for_projects > 1
		}
		custom_cost_text = decision_cost_civ_factory_1
		
		fire_only_once = no
		days_remove = 180
		
		modifier = {
			civilian_factory_use = 1
		}

		complete_effect = {
			FROM = {
				set_state_category = pastoral
			}
			POL = {
				transfer_state = FROM
			}			
		}

		ai_will_do = {
			factor = 10
		}

	}
}





