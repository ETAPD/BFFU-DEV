# Decisions for the "Racial Harmony" balance of power (ported from New Ways).
# The MAN_jews_are_another_race focus gate was removed (focus blocks come later).
# These appear inside the BoP window.
MAN_racial_harmony_category_bop = {

	# Shift toward Chinese influence
	MAN_promote_chinese_in_concordia_association = {
		icon = MAN_promote_chinese_in_concordia_association

		cost = 30

		days_remove = 1
		days_re_enable = 30

		remove_effect = {
			add_power_balance_value = {
				id = MAN_racial_harmony
				value = -0.01
			}
		}
	}

	# Shift toward Japanese influence
	MAN_promote_japanese_in_concordia_association = {
		icon = MAN_promote_japanese_in_concordia_association

		cost = 30

		days_remove = 1
		days_re_enable = 30

		remove_effect = {
			add_power_balance_value = {
				id = MAN_racial_harmony
				value = 0.01
			}
		}
	}

	# Minor peoples — only relevant when the balance has swung to an extreme,
	# nudging it back toward the centre.
	MAN_promote_koreans_in_concordia_association = {
		icon = MAN_promote_koreans_in_concordia_association

		available = {
			power_balance_value = {
				id = MAN_racial_harmony
				value < -0.3
			}
		}

		cost = 60

		days_remove = 1
		days_re_enable = 50

		remove_effect = {
			add_power_balance_value = {
				id = MAN_racial_harmony
				value = 0.01
			}
		}
	}

	MAN_promote_mongols_in_concordia_association = {
		icon = MAN_promote_mongols_in_concordia_association

		available = {
			OR = {
				power_balance_value = { id = MAN_racial_harmony value < -0.3 }
				power_balance_value = { id = MAN_racial_harmony value > 0.3 }
			}
		}

		cost = 40

		days_remove = 1
		days_re_enable = 45

		remove_effect = {
			if = {
				limit = { power_balance_value = { id = MAN_racial_harmony value > 0 } }
				add_power_balance_value = { id = MAN_racial_harmony value = -0.01 }
			}
			else = {
				add_power_balance_value = { id = MAN_racial_harmony value = 0.01 }
			}
		}
	}

	MAN_promote_manchus_in_concordia_association = {
		icon = MAN_promote_manchus_in_concordia_association

		available = {
			OR = {
				power_balance_value = { id = MAN_racial_harmony value < -0.3 }
				power_balance_value = { id = MAN_racial_harmony value > 0.3 }
			}
		}

		cost = 50

		days_remove = 1
		days_re_enable = 60

		remove_effect = {
			if = {
				limit = { power_balance_value = { id = MAN_racial_harmony value > 0 } }
				add_power_balance_value = { id = MAN_racial_harmony value = -0.01 }
			}
			else = {
				add_power_balance_value = { id = MAN_racial_harmony value = 0.01 }
			}
		}
	}

	MAN_promote_russians_in_concordia_association = {
		icon = MAN_promote_russians_in_concordia_association

		available = {
			OR = {
				power_balance_value = { id = MAN_racial_harmony value < -0.3 }
				power_balance_value = { id = MAN_racial_harmony value > 0.3 }
			}
		}

		cost = 75

		days_remove = 1
		days_re_enable = 50

		remove_effect = {
			if = {
				limit = { power_balance_value = { id = MAN_racial_harmony value > 0 } }
				add_power_balance_value = { id = MAN_racial_harmony value = -0.01 }
			}
			else = {
				add_power_balance_value = { id = MAN_racial_harmony value = 0.01 }
			}
		}
	}

	MAN_promote_jews_in_concordia_association = {
		icon = MAN_promote_jews_in_concordia_association

		available = {
			OR = {
				power_balance_value = { id = MAN_racial_harmony value < -0.3 }
				power_balance_value = { id = MAN_racial_harmony value > 0.3 }
			}
		}

		cost = 35

		days_remove = 1
		days_re_enable = 50

		remove_effect = {
			if = {
				limit = { power_balance_value = { id = MAN_racial_harmony value > 0 } }
				add_power_balance_value = { id = MAN_racial_harmony value = -0.01 }
			}
			else = {
				add_power_balance_value = { id = MAN_racial_harmony value = 0.01 }
			}
		}
	}
}
