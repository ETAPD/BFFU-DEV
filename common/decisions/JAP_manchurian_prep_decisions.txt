
JAP_manchurian_preparations = {

	# Railways to level 5 + a supply hub
	JAP_manchuria_railways_and_hub = {
		icon = GFX_decision_generic_industry
		allowed = { original_tag = JAP }
		fire_only_once = yes
		visible = { NOT = { has_war_with = CHI } }
		available = { NOT = { has_war_with = CHI } }
		cost = 75
		days_remove = 90
		complete_effect = {
			add_offsite_building = { type = industrial_complex level = -5 } # tie up 5 civilian factories
			custom_effect_tooltip = JAP_manchuria_uses_5_civ_tt
		}
		remove_effect = {
			add_offsite_building = { type = industrial_complex level = 5 } # release the 5 civilian factories
			609 = { set_building_level = { type = supply_node province = 12043 level = 1 instant_build = yes } }
			609 = { build_railway = { level = 5 path = { 13379 9768 3955 6969 11822 12043 1052 3900 9905 11886 6837 9835 11815 6951 11771 11808 916 4642 4572 } } } # state scope required for build_railway
		}
		ai_will_do = { factor = 1 }
	}

	# Level-5 land forts across every Japanese/Manchukuoan state bordering China.
	JAP_manchuria_border_forts = {
		icon = GFX_decision_generic_construction
		allowed = { original_tag = JAP }
		fire_only_once = yes
		visible = { NOT = { has_war_with = CHI } }
		available = { NOT = { has_war_with = CHI } }
		cost = 25
		days_remove = 60
		complete_effect = {
			add_offsite_building = { type = industrial_complex level = -5 } # tie up 5 civilian factories
			custom_effect_tooltip = JAP_manchuria_uses_5_civ_tt
		}
		remove_effect = {
			add_offsite_building = { type = industrial_complex level = 5 } # release the 5 civilian factories
			every_state = {
				limit = {
					OR = { is_owned_by = JAP is_owned_by = MAN is_controlled_by = JAP is_controlled_by = MAN }
					any_neighbor_state = { is_core_of = CHI NOT = { OR = { is_controlled_by = JAP is_controlled_by = MAN } } }
				}
				set_building_level = { type = land_fort level = 5 instant_build = yes }
			}
		}
		ai_will_do = { factor = 1 }
	}

	# Level-5 airfields across every Japanese/Manchukuoan state bordering China.
	JAP_manchuria_border_airfields = {
		icon = GFX_decision_generic_air_production
		allowed = { original_tag = JAP }
		fire_only_once = yes
		visible = { NOT = { has_war_with = CHI } }
		available = { NOT = { has_war_with = CHI } }
		cost = 25
		days_remove = 60
		complete_effect = {
			add_offsite_building = { type = industrial_complex level = -5 } # tie up 5 civilian factories
			custom_effect_tooltip = JAP_manchuria_uses_5_civ_tt
		}
		remove_effect = {
			add_offsite_building = { type = industrial_complex level = 5 } # release the 5 civilian factories
			every_state = {
				limit = {
					OR = { is_owned_by = JAP is_owned_by = MAN is_controlled_by = JAP is_controlled_by = MAN }
					any_neighbor_state = { is_core_of = CHI NOT = { OR = { is_controlled_by = JAP is_controlled_by = MAN } } }
				}
				set_building_level = { type = air_base level = 5 instant_build = yes }
			}
		}
		ai_will_do = { factor = 1 }
	}

	# Arms shipment to Manchukuo
	JAP_manchuria_arms_shipment = {
		icon = GFX_decision_generic_military
		allowed = { original_tag = JAP }
		fire_only_once = yes
		visible = { NOT = { has_war_with = CHI } MAN = { exists = yes } }
		available = { NOT = { has_war_with = CHI } MAN = { exists = yes } }
		cost = 50
		days_remove = 60
		complete_effect = {
			add_offsite_building = { type = industrial_complex level = -5 } # tie up 5 civilian factories
			custom_effect_tooltip = JAP_manchuria_uses_5_civ_tt
			MAN = {
				add_equipment_to_stockpile = { type = infantry_equipment amount = 10000 producer = JAP }
				add_equipment_to_stockpile = { type = squad_equipment amount = 4000 producer = JAP }
				add_equipment_to_stockpile = { type = support_equipment amount = 1500 producer = JAP }
				add_equipment_to_stockpile = { type = train_equipment amount = 25 producer = JAP }
				add_equipment_to_stockpile = { type = motorized_equipment amount = 700 producer = JAP }
			}
		}
		remove_effect = {
			add_offsite_building = { type = industrial_complex level = 5 } # release the 5 civilian factories
		}
		ai_will_do = { factor = 1 }
	}
}
