# Kwantung Escalation - city objectives during the war with China (opens 7 Jul 1937).
# Each city is a mission with a desired capture window:
#   - Taken too early: world tension (Nanjing none, Guangzhou/Wuhan +5%, Chongqing +30%).
#   - Taken during the window: world tension (Guangzhou/Wuhan +2%, others nothing).
#   - Not taken by the window's end (mission timeout): -7.5% stability + a Kwantung Desires
#     spirit for that front (+10% attack, +10% breakthrough).
# Timeouts run from the war's start (7 Jul 1937). Windows now: Nanjing ~Jul-Oct 1937, Guangzhou ~Sep 1937-Mar 1938, Wuhan ~Dec 1937-Apr 1938, Chongqing ~Jul-Oct 1938.

JAP_kwantung_escalation = {

	# Nanjing (state 1035) - desired Jul-Oct 1937, no early penalty. Timeout ~7 Oct 1937 (92d from war start).
	JAP_take_nanjing_mission = {
		icon = GFX_decision_generic_military
		allowed = { original_tag = JAP }
		visible = { has_war_with = CHI }
		activation = { has_war_with = CHI date > 1937.7.6 }
		available = { 1035 = { is_controlled_by = JAP } }
		days_mission_timeout = 92
		fire_only_once = yes
		is_good = yes
		cancel_trigger = { NOT = { has_war_with = CHI } }
		timeout_effect = {
			add_stability = -0.075
			1035 = { add_dynamic_modifier = { modifier = JAP_kwantung_desires } }
		}
		ai_will_do = { factor = 1 }
	}

	# Guangzhou (state 592) - early before Sep 1937; desired Sep 1937-Mar 1938. Timeout ~9 Mar 1938 (245d from war start).
	JAP_take_guangzhou_mission = {
		icon = GFX_decision_generic_military
		allowed = { original_tag = JAP }
		visible = { has_war_with = CHI }
		activation = { has_war_with = CHI date > 1937.7.6 }
		available = { 592 = { is_controlled_by = JAP } }
		days_mission_timeout = 245
		fire_only_once = yes
		is_good = yes
		cancel_trigger = { NOT = { has_war_with = CHI } }
		complete_effect = {
			if = {
				limit = { NOT = { date > 1937.9.6 } } # taken early (before ~Sep 1937)
				add_named_threat = { threat = 5 name = JAP_kwantung_escalation }
			}
			else = {
				add_named_threat = { threat = 2 name = JAP_kwantung_escalation } # taken on time
			}
		}
		timeout_effect = {
			add_stability = -0.075
			592 = { add_dynamic_modifier = { modifier = JAP_kwantung_desires } }
		}
		ai_will_do = { factor = 1 }
	}

	# Wuhan (state 1036) - early before Dec 1937; desired Dec 1937-Apr 1938. Timeout ~8 Apr 1938 (275d from war start).
	JAP_take_wuhan_mission = {
		icon = GFX_decision_generic_military
		allowed = { original_tag = JAP }
		visible = { has_war_with = CHI }
		activation = { has_war_with = CHI date > 1937.7.6 }
		available = { 1036 = { is_controlled_by = JAP } }
		days_mission_timeout = 275
		fire_only_once = yes
		is_good = yes
		cancel_trigger = { NOT = { has_war_with = CHI } }
		complete_effect = {
			if = {
				limit = { NOT = { date > 1937.12.7 } } # taken early (before ~Dec 1937)
				add_named_threat = { threat = 5 name = JAP_kwantung_escalation }
			}
			else = {
				add_named_threat = { threat = 2 name = JAP_kwantung_escalation } # taken on time
			}
		}
		timeout_effect = {
			add_stability = -0.075
			1036 = { add_dynamic_modifier = { modifier = JAP_kwantung_desires } }
		}
		ai_will_do = { factor = 1 }
	}

	# Chongqing (state 1037) - early before Jul 1938 = +30% tension; desired Jul-Oct 1938. Timeout ~7 Oct 1938 (457d from war start).
	JAP_take_chongqing_mission = {
		icon = GFX_decision_generic_military
		allowed = { original_tag = JAP }
		visible = { has_war_with = CHI }
		activation = { has_war_with = CHI date > 1937.7.6 }
		available = { 1037 = { is_controlled_by = JAP } }
		days_mission_timeout = 457
		fire_only_once = yes
		is_good = yes
		cancel_trigger = { NOT = { has_war_with = CHI } }
		complete_effect = {
			if = {
				limit = { NOT = { date > 1938.7.9 } } # taken before ~Jul 1938
				add_named_threat = { threat = 30 name = JAP_kwantung_escalation }
			}
		}
		timeout_effect = {
			add_stability = -0.075
			1037 = { add_dynamic_modifier = { modifier = JAP_kwantung_desires } }
		}
		ai_will_do = { factor = 1 }
	}
}
