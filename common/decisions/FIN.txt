FIN_five_year_plan_category = {

	FIN_develop_helsinki = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_helsinki_done }
			}
			111 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 111 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 111 }
		}

		visible = { NOT = { has_country_flag = FIN_helsinki_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_helsinki_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_industrial_capacity_factory = 0.05 tooltip = industrial_capacity_factory_tt }
			add_to_variable = { FIN_industry_repair_factor = 0.2 tooltip = industry_repair_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = 0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = 0.075 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_pori = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_pori_done }
			}
			926 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 926 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 926 }
		}

		visible = { NOT = { has_country_flag = FIN_pori_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_pori_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_conversion_cost_civ_to_mil_factor = -0.15 tooltip = conversion_cost_civ_to_mil_factor_tt }
			add_to_variable = { FIN_production_speed_industrial_complex_factor = 0.15 tooltip = production_speed_industrial_complex_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = -0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = -0.05 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_vassa = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_vassa_done }
			}
			149 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 149 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 149 }
		}

		visible = { NOT = { has_country_flag = FIN_vassa_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_vassa_done
			
			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_production_factory_max_efficiency_factor = 0.025 tooltip = production_factory_max_efficiency_factor_tt }
			add_to_variable = { FIN_production_factory_efficiency_gain_factor = 0.05 tooltip = production_factory_efficiency_gain_factor_tt }
			add_to_variable = { FIN_line_change_production_efficiency_factor = 0.05 tooltip = line_change_production_efficiency_factor_tt }
			add_to_variable = { FIN_production_factory_start_efficiency_factor = 0.025 tooltip = production_factory_start_efficiency_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = 0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = 0.075 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_kuopio = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0  }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_kuopio_done }
			}
			150 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 150 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 150 }
		}

		visible = { NOT = { has_country_flag = FIN_kuopio_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_kuopio_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_production_factory_max_efficiency_factor = 0.025 tooltip = production_factory_max_efficiency_factor_tt }
			add_to_variable = { FIN_production_factory_efficiency_gain_factor = 0.025 tooltip = production_factory_efficiency_gain_factor_tt }
			add_to_variable = { FIN_line_change_production_efficiency_factor = 0.025 tooltip = line_change_production_efficiency_factor_tt }
			add_to_variable = { FIN_production_factory_start_efficiency_factor = 0.025 tooltip = production_factory_start_efficiency_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = 0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = 0.075 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_oulu = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_oulu_done }
			}
			929 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 929 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 929 }
		}

		visible = { NOT = { has_country_flag = FIN_oulu_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_oulu_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_local_resources_factor = 0.05 tooltip = local_resources_factor_tt }
			add_to_variable = { FIN_production_speed_arms_factory_factor = 0.075 tooltip = production_speed_arms_factory_factor_tt  }
			add_to_variable = { FIN_production_speed_industrial_complex_factor = -0.05 tooltip = production_speed_industrial_complex_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = 0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = 0.075 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_mikkeli = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_mikkeli_done }
			}
			930 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 930 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 930 }
		}

		visible = { NOT = { has_country_flag = FIN_mikkeli_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_mikkeli_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_min_export = -0.1 tooltip = min_export_tt }
			add_to_variable = { FIN_monthly_population = 0.5 tooltip = monthly_population_tt  }
			add_to_variable = { FIN_production_speed_industrial_complex_factor = 0.15 tooltip = production_speed_industrial_complex_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = -0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = -0.05 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_develop_lappi = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_lappi_done }
			}
			148 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 148 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 148 }
		}

		visible = { NOT = { has_country_flag = FIN_lappi_done } }

		cost = 50
		days_remove = 75

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_lappi_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			custom_effect_tooltip = FIN_gain_progress_tt

			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_local_resources_factor = 0.05 tooltip = local_resources_factor_tt }
			add_to_variable = { FIN_production_speed_arms_factory_factor = 0.075 tooltip = production_speed_arms_factory_factor_tt  }
			add_to_variable = { FIN_production_speed_industrial_complex_factor = -0.05 tooltip = production_speed_industrial_complex_factor_tt }
			add_to_variable = { FIN_consumer_goods_factor = 0.075 tooltip = consumer_goods_factor_tt }
			add_to_variable = { FIN_cg_points = 0.075 }

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_expand_civilian_sector = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_2_tt
				has_country_flag = FIN_has_development_points2
			}
			check_variable = { FIN_development_points > 1 }
		}

		visible = {
			check_variable = {
				FIN_civilian_sector_expanded < 10
			}
		}

		cost = 50
		days_remove = 80

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -2 }
			add_to_variable = { FIN_civilian_sector_expanded = 1 }

			custom_effect_tooltip = FIN_development_points_remove_tt2
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			custom_effect_tooltip = FIN_gain_progress_tt
			custom_effect_tooltip = FIN_expand_civilian_sector_tt
			random_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_expand_military_sector = {
		icon = GFX_decision_generic_construction

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_2_tt
				has_country_flag = FIN_has_development_points2
			}
			check_variable = { FIN_development_points > 1 }
		}

		visible = {
			check_variable = {
				FIN_military_sector_expanded < 10
			}
		}

		cost = 50
		days_remove = 80

		complete_effect = {
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -2 }
			add_to_variable = { FIN_military_sector_expanded = 1 }

			custom_effect_tooltip = FIN_development_points_remove_tt2
			custom_effect_tooltip = generic_skip_one_line_tt
		}

		remove_effect = {
			custom_effect_tooltip = FIN_gain_progress_tt
			custom_effect_tooltip = FIN_expand_military_sector_tt
			random_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}

			custom_effect_tooltip = generic_skip_one_line_tt
		}

		ai_will_do = { factor = 1 }
	}

	FIN_gain_development = {
		icon = GFX_decision_generic_construction
		allowed = {
			tag = FIN
		}

		activation = {
			has_completed_focus = FIN_five_year_plan
		}

		visible = {
        	has_completed_focus = FIN_five_year_plan
    	}

		available = {
			hidden_trigger = { always = no }
		}

		is_good = no

		days_mission_timeout = FIN_development_days

		timeout_effect = {
			custom_effect_tooltip = FIN_space_tt
			custom_effect_tooltip = FIN_development_points_add_tt
			add_to_variable = { FIN_development_points = 1 }
			custom_effect_tooltip = FIN_space_tt
			hidden_effect = {
				activate_mission = FIN_gain_development
			}
		}
	}

	FIN_gain_consumer_goods = {
		icon = GFX_decision_generic_construction
		allowed = {
			tag = FIN
		}

		activation = {
			has_completed_focus = FIN_five_year_plan
			NOT = {
				has_completed_focus = FIN_finish_five_year_plan
			}
		}

		visible = {
			NOT = { has_country_flag = FIN_done_five_year_plan }
        	has_completed_focus = FIN_five_year_plan
    	}

		available = {
			NOT = { has_country_flag = FIN_done_five_year_plan }
			hidden_trigger = { always = no }
		}

		is_good = no

		days_mission_timeout = 40

		timeout_effect = {
			custom_effect_tooltip = FIN_space_tt
			custom_effect_tooltip = FIN_finnish_industry_tt
			add_to_variable = { FIN_consumer_goods_factor = 0.025 tooltip = consumer_goods_factor_tt}
			add_to_variable = { FIN_cg_points = 0.025 }
			custom_effect_tooltip = FIN_space_tt
			hidden_effect = {
				activate_mission = FIN_gain_consumer_goods
			}
		}
	}

}


FIN_aland_category = {
	#ALAND

	FIN_develop_mariehamn = {
		icon = GFX_decision_generic_construction

		visible = {
			has_completed_focus = FIN_german_investments_in_aland
		}

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_mariehamn_done }
			}
			145 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 145 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 145 }
		}

		visible = { NOT = { has_country_flag = FIN_mariehamn_done } }

		cost = 50
		days_remove = 90

		complete_effect = {
			custom_effect_tooltip = FIN_gain_progress_tt
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt

			set_country_flag = any_decision_active
			set_country_flag = FIN_mariehamn_done
		}

		remove_effect = {
			clr_country_flag = any_decision_active
			
			145 = {
				add_dynamic_modifier = {
					modifier = FIN_aland_economy_1
				}
	
			}
		}

		ai_will_do = { factor = 1 }
	}

	FIN_open_aland_bank = {
		icon = GFX_decision_generic_construction

		visible = {
			has_completed_focus = FIN_open_aland_bank
		}

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			has_country_flag = FIN_mariehamn_done
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_aland_bank_done }
			}
			145 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 145 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 145 }
		}

		visible = { NOT = { has_country_flag = FIN_aland_bank_done } }

		cost = 50
		days_remove = 90

		complete_effect = {
			custom_effect_tooltip = FIN_gain_progress_tt
			add_to_variable = { FIN_five_year_plan_progress = 1 }
			add_to_variable = { FIN_development_points = -1 }

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt

			set_country_flag = any_decision_active
			set_country_flag = FIN_aland_bank_done
		}

		remove_effect = {
			clr_country_flag = any_decision_active

			145 = {
				hidden_effect = {
					remove_dynamic_modifier = {
						modifier = FIN_aland_economy_1
					}
				}
				add_dynamic_modifier = {
					modifier = FIN_aland_economy_2
				}
			}
		}

		ai_will_do = { factor = 1 }
	}

	FIN_defense_of_aland = {
		icon = GFX_decision_generic_construction

		visible = {
			has_completed_focus = FIN_aland_strategy
		}

		available = {
			custom_trigger_tooltip = { 
				tooltip = FIN_development_points_tt
				has_country_flag = FIN_has_development_points
			}
			check_variable = { FIN_development_points > 0 }
			hidden_trigger = {
				NOT = { has_country_flag = any_decision_active }
				NOT = { has_country_flag = FIN_aland_defense_done }
			}
			145 = { is_fully_controlled_by = ROOT }
		}

		cancel_trigger = {
			NOT = { 145 = { is_controlled_by = ROOT } }
		}

		highlight_states = {
			highlight_state_targets = { state = 145 }
		}

		visible = { NOT = { has_country_flag = FIN_aland_defense_done } }

		cost = 50
		days_remove = 90

		complete_effect = {
			add_to_variable = { FIN_development_points = -1 }
			set_country_flag = any_decision_active
			set_country_flag = FIN_aland_defense_done

			custom_effect_tooltip = FIN_development_points_remove_tt
			custom_effect_tooltip = generic_skip_one_line_tt


		}

		remove_effect = {
			clr_country_flag = any_decision_active
				145 = {
					add_dynamic_modifier = {
						modifier = FIN_defence_of_aland
					}
				}

		}

		ai_will_do = { factor = 1 }
	}

}

FIN_winter_war = {

	FIN_proclaim_victory = {
		icon = GFX_decision_generic_construction

		allowed = {
			tag = FIN
		}

		visible = {
			has_war_with = SOV
			NOT = {
            OR = {
					has_country_flag = FIN_won_winter_war
					has_country_flag = FIN_lost_winter_war_left
					has_country_flag = FIN_lost_winter_war_right
            	}
    		}
		}

		days_remove = 7

		available = {
			OR = {
				controls_state = 195
				SOV = {
					casualties > 200000
				}
			}
		}
		remove_effect = {
			hidden_effect = {
				white_peace = SOV
				remove_mission = FIN_ongoing_winter_war
				complete_national_focus = FIN_national_humiliation
				complete_national_focus = FIN_a_reliable_ally
			}
			set_country_flag = FIN_won_winter_war
			custom_effect_tooltip = FIN_victory_in_winter_war
		}
	}

	FIN_ongoing_winter_war = {
		icon = GFX_decision_generic_construction

		allowed = {
			tag = FIN
		}

		activation = {
			NOT = {
				OR = {
					has_country_flag = FIN_won_winter_war
					has_country_flag = FIN_lost_winter_war_left
					has_country_flag = FIN_lost_winter_war_right
        		}
    		}
			has_war_with = SOV

		}

		visible = {
			has_war_with = SOV
			NOT = {
				OR = {
					has_country_flag = FIN_won_winter_war
					has_country_flag = FIN_lost_winter_war_left
					has_country_flag = FIN_lost_winter_war_right
				}
        	}	
		}
		
		

		available = {
			OR = {
				NOT = {
					has_war_with = SOV
				}
			surrender_progress > 0.25
			NOT = { controls_province = 9206 }
			}
		}

		days_mission_timeout = 210

		is_good = no

		complete_effect = {
			custom_effect_tooltip = FIN_loss_in_winter_war

			hidden_effect = {
				white_peace = SOV
				SOV = {
					transfer_state = 146
					transfer_state = 147
				}
				if = {
					limit = { has_completed_focus = FIN_secure_german_support }
						set_country_flag = FIN_lost_winter_war_left
						complete_national_focus = FIN_national_humiliation
						complete_national_focus = FIN_total_german_takeover
				}
				if = {
					limit = { has_completed_focus = FIN_consolidate_finnish_authority }
						set_country_flag = FIN_lost_winter_war_right
						complete_national_focus = FIN_national_humiliation
						complete_national_focus = FIN_a_military_coup
				}
			}
		}

		timeout_effect = {
			hidden_effect = {
				white_peace = SOV
				complete_national_focus = FIN_national_humiliation
				complete_national_focus = FIN_a_reliable_ally
			}
			set_country_flag = FIN_won_winter_war
			custom_effect_tooltip = FIN_victory_in_winter_war
		}
	}

	FIN_evacuation_of_karelia = {
		icon = GFX_decision_generic_construction

		allowed = {
			tag = FIN
		}

		activation = {
			NOT = {
				OR = {
					has_country_flag = FIN_won_winter_war
					has_country_flag = FIN_lost_winter_war_left
					has_country_flag = FIN_lost_winter_war_right
        		}
    		}
			has_completed_focus = FIN_evacuate_karjala
			has_war_with = SOV

		}

		visible = {
			has_war_with = SOV
			has_completed_focus = FIN_evacuate_karjala
			NOT = {
				OR = {
					has_country_flag = FIN_won_winter_war
					has_country_flag = FIN_lost_winter_war_left
					has_country_flag = FIN_lost_winter_war_right
				}
        	}	
		}
		
		

		available = {
			OR = {
				NOT = {
					has_war_with = SOV
				}
			surrender_progress > 0.25
			}
		}

		days_mission_timeout = 30

		is_good = no

		complete_effect = {
			
		}

		timeout_effect = {
			if = {
				limit = { 
					146 = {
						num_of_civilian_factories > 0
					}
				}
				146 = {
					remove_building = {
						type = arms_factory
						level = 1
					}	
					remove_building = {
						type = industrial_complex
						level = 1
					}
					add_manpower = -35000	
				}
				111 = {
					add_extra_state_shared_building_slots = 2
					add_building_construction = {
						type = industrial_complex
						level = 1
						instant_build = yes
					}
					add_building_construction = {
						type = arms_factory
						level = 1
						instant_build = yes
					}
					add_manpower = 35000
				}
				hidden_effect = {
					activate_mission = FIN_evacuation_of_karelia
				}
			}
			custom_effect_tooltip = FIN_evac
		}
	}
}

propaganda_efforts = {

	#SS SPAWN INS

	FIN_ss_spawn = {
		icon = GFX_decision_generic_construction

		allowed = {
			tag = FIN
		}

		activation = {
			has_completed_focus = FIN_german_ss_cooperation
			check_variable = { FIN_ss_volunteers < 6 }
		}

		visible = {
        	has_completed_focus = FIN_german_ss_cooperation
			check_variable = { FIN_ss_volunteers < 6 }
    	}

		available = {
			always = no
		}

		is_good = yes

		days_mission_timeout = 180

		timeout_effect = {
			custom_effect_tooltip = FIN_space_tt
			custom_effect_tooltip = FIN_ss_volunteer_wave_tt
			custom_effect_tooltip = FIN_space_tt
			add_to_variable = { FIN_ss_volunteers = 1 }
			if = {
				limit = {
					check_variable = { FIN_ss_volunteers < 6 }
				}
				hidden_effect = {
					activate_mission = FIN_ss_spawn
					111 = {
						create_unit = {
							division = "division_template = \"Suomalainen Waffen-SS:n vapaaehtoispataljoona\" start_experience_factor = 2.0 start_equipment_factor = 1.0"
							owner = ROOT
							count = 1
						}
					}
				}
			}
		}
	}

	FIN_force_join_war = {
		priority = 100
		allowed = { tag = FIN }

		icon = generic_prepare_civil_war

		available = {
			hidden_trigger = { always = no }
		}

		days_mission_timeout = 180
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			OR = {
				has_war_with = SOV
				has_war_with = MON
			}
		}

		visible = {
			always = yes
		}

		activation = {
			GER = {
				has_war_with = SOV
			}
		}
		
		timeout_effect = {
			add_war_support = -0.5
			add_stability =  -0.5
			set_global_flag = FIN_not_at_war
			FIN = {country_event = { id = finland.503 }}
			SOV = {
				create_wargoal = {
					type = puppet_wargoal_focus
					target = FIN
				}
			}
			SOV = {country_event = { id =soviet.504 }}
		}
	}


	FIN_recieve_german_support = {

		icon = GFX_decision_generic_construction

		allowed = {
			tag = FIN
		}

		visible = {
        	has_completed_focus = FIN_hand_over_leningrad_to_our_masters
    	}

		available = {
			has_completed_focus = FIN_hand_over_leningrad_to_our_masters
			date > 1942.6.1
			SOV = {
				controls_state = 206
				controls_state = 202
				controls_state = 808
			}
		}

		remove_effect = {
			add_timed_idea = {
				idea = FIN_german_funds
				days = 30
			}
		}
	}
}
