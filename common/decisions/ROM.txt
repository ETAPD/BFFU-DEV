operations = {

	# Less casualties
	ROM_medicine_caches = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_1}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			NOT = { has_country_flag = ROM_innovation_1 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			add_ideas = ROM_military_innovations_a
			set_country_flag = ROM_innovation_1
			custom_effect_tooltip = generic_skip_one_line_tt
			unlock_decision_tooltip = { decision = ROM_fortified_positions show_effect_tooltip = no }
			unlock_decision_tooltip = { decision = ROM_escape_routes show_effect_tooltip = no }
		}
	}

	# Less supply usage
	ROM_supply_caches = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_1}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			NOT = { has_country_flag = ROM_innovation_1 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt

			add_ideas = ROM_military_innovations_b
			set_country_flag = ROM_innovation_1
			custom_effect_tooltip = generic_skip_one_line_tt

			unlock_decision_tooltip = { decision = ROM_fortified_positions show_effect_tooltip = no }
			unlock_decision_tooltip = { decision = ROM_escape_routes show_effect_tooltip = no }
		}
	}

	# Defense
	ROM_fortified_positions = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_2}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			has_country_flag = ROM_innovation_1
			NOT = { has_country_flag = ROM_innovation_2 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a
					add_idea = ROM_military_innovations_a_a
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b
					add_idea = ROM_military_innovations_b_a
				}
			}
			IF = {
				limit = {
					NOT = { has_country_flag = ROM_innovation_1 }
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b
					add_idea = ROM_military_innovations_b_a
				}
			}
			set_country_flag = ROM_innovation_2
			custom_effect_tooltip = generic_skip_one_line_tt

			unlock_decision_tooltip = { decision = ROM_fortified_positions show_effect_tooltip = no }
			unlock_decision_tooltip = { decision = ROM_escape_routes show_effect_tooltip = no }
		}
	}

	# Retreat speed
	ROM_escape_routes = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_2}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			has_country_flag = ROM_innovation_1
			NOT = { has_country_flag = ROM_innovation_2 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a
					add_idea = ROM_military_innovations_a_b
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b
					add_idea = ROM_military_innovations_b_b
				}
			}
			IF = {
				limit = {
					NOT = { has_country_flag = ROM_innovation_1 }
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b
					add_idea = ROM_military_innovations_b_b
				}
			}
			set_country_flag = ROM_innovation_2
			custom_effect_tooltip = generic_skip_one_line_tt

			unlock_decision_tooltip = { decision = ROM_fortified_positions show_effect_tooltip = no }
			unlock_decision_tooltip = { decision = ROM_escape_routes show_effect_tooltip = no }
		}
	}

	# Organization, max planning
	ROM_strict_command = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_3}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			has_country_flag = ROM_innovation_2
			NOT = { has_country_flag = ROM_innovation_3 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_a
					add_idea = ROM_military_innovations_a_a_a
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_b
					add_idea = ROM_military_innovations_a_b_a
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b_a
					add_idea = ROM_military_innovations_b_a_a
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b_b
					add_idea = ROM_military_innovations_b_b_a
				}
			}
			IF = {
				limit = {
					NOT = { has_country_flag = ROM_innovation_2 }
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_a
					add_idea = ROM_military_innovations_a_a_a
				}
			}
			set_country_flag = ROM_innovation_3
		}
	}

	# Recovery rate, planning speed
	ROM_delegated_command = {
		allowed = {
			original_tag = ROM
		}

		visible = {
			NOT = {has_country_flag = ROM_innovation_3}
		}

		available = {
			ROM = { has_completed_focus = ROM_military_innovations }
			has_country_flag = ROM_innovation_2
			NOT = { has_country_flag = ROM_innovation_3 }
			NOT = { has_idea = ROM_military_innovations_funding }
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = ROM_military_innovations_funding
				days = 180
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_a
					add_idea = ROM_military_innovations_a_a_b
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_a_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_b
					add_idea = ROM_military_innovations_a_b_b
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b_a
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b_a
					add_idea = ROM_military_innovations_b_a_b
				}
			}
			IF = {
				limit = {
					has_idea = ROM_military_innovations_b_b
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_b_b
					add_idea = ROM_military_innovations_b_b_b
				}
			}
			IF = {
				limit = {
					NOT = { has_country_flag = ROM_innovation_2 }
				}
				swap_ideas = {
					remove_idea = ROM_military_innovations_a_a
					add_idea = ROM_military_innovations_a_a_b
				}
			}
			set_country_flag = ROM_innovation_3
		}
	}

	# ROM_expand_greater_romania = {

	# 	icon = generic_nationalism


	# 	fire_only_once = yes
		
	# 	available = {


	ROM_reclaim_territory = {

		icon = generic_nationalism

		allowed = {
			original_tag = ROM
		}

		fire_only_once = yes
		
		available = {
			SOV = {
				owns_state = 80
				owns_state = 78
				owns_state = 766
			}
			has_full_control_of_state = 80
			has_full_control_of_state = 78
			has_full_control_of_state = 766
		}

		visible = {
			SOV = {
				owns_state = 80
				owns_state = 78
				owns_state = 766
			}
		}
		
		complete_effect = {
			add_political_power = 100
			add_stability = 0.1
			transfer_state = 80
			transfer_state = 78
			transfer_state = 766
		}
	}
	
	ROM_michaels_coup = {

		icon = generic_tank

		available = {
			GER = { divisions_in_state = { state = 46 size > 3 } }
		}

		days_mission_timeout = 7
		fire_only_once = yes

		activation = {
			always = no
		}

		complete_effect = {
			every_state = {
				limit = {
					is_fully_controlled_by = ROM
					NOT = { is_owned_by = ROM }
				}
				transfer_state_to = GER
			}
			set_autonomy = {
				target = ROM
				autonomy_state = autonomy_reichsprotectorate
			}
		}

		is_good = no

		timeout_effect = {
			SOV = {
				custom_effect_tooltip = needs_approval
				country_event = DOD_romania.501
			}
			effect_tooltip = {
				ROM = {
					leave_faction = yes
					white_peace = SOV
					every_country = {
						limit = {
							has_war_with = ROM
							is_in_faction_with = ENG
						}
						white_peace = ROM
					}
				}
				SOV = { 
					add_to_faction = ROM
					transfer_state = 78
					transfer_state = 766
					transfer_state = 80
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = ROM
				}
			}
		}
	}
	
	ROM_surrender_soviets = {
		icon = generic_nationalism

		allowed ={
			original_tag = ROM
		}

		fire_only_once = yes
		
		visible = {
			SOV = { has_active_mission = SOV_besserabia_war }
		}

		days_remove = 1

		available = {
			has_war_with = SOV
			has_civil_war = no
			is_in_faction = no
			OR = {
				casualties > 40000
				surrender_progress > 0.20
				NOT = {
					79 = {
						CONTROLLER = {
							OR = {
								tag = ROM
								is_subject_of = ROM
							}
						}
					}
				}
			}
		}

		remove_effect = {
			SOV = {
				white_peace = ROM
 				transfer_state = 78
				transfer_state = 80
 				transfer_state = 766
			}
			ROM = { set_country_flag = bessarabia_finished }
			ROM = { set_country_flag = ROM_lost_bessarabia }
			diplomatic_relation = {
				country = SOV
				relation = non_aggression_pact
			}
			IF = {
				limit = {
					NOT = { has_completed_focus = ROM_antonescu_takes_control }
	 				NOT = { has_completed_focus = ROM_carols_triumph }
				}
				complete_national_focus = ROM_antonescu_takes_control
			}
		}
		ai_will_do = {
			factor = 20
		}
	}
	
	ROM_victory_soviets = {
		icon = generic_nationalism
		allowed = {
			original_tag = ROM
		}
		fire_only_once = yes
		visible = {
			SOV = { has_active_mission = SOV_besserabia_war }
		}
		fire_only_once = yes

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = {
			192
    	}
		highlight_states = { 
			highlight_state_targets = {
				state = 192
				state = 834
			}
		}

		available = {
			has_war_with = SOV
			NOT = { has_war_with = GER }
			OR = {
				SOV = {
					casualties > 250000
				}
				AND = {
					192 = {
						CONTROLLER = {
							OR = {
								tag = ROM
								is_subject_of = ROM
							}
						}
					}
					834 = {
						CONTROLLER = {
							OR = {
								tag = ROM
								is_subject_of = ROM
							}
						}
					}
				}
				
			}
		}
		days_remove = 7
		remove_effect = {
			hidden_effect = {
				SOV = {
					white_peace = ROM
					country_event = { days = 1 id = soviet.677 }
				}
			}
			ROM = { 
				set_country_flag = bessarabia_finished
				set_country_flag = ROM_won_bessarabia
			}
			diplomatic_relation = {
				country = SOV
				relation = non_aggression_pact
			}
			IF = {
				limit = {
					NOT = { has_completed_focus = ROM_antonescu_takes_control }
	 				NOT = { has_completed_focus = ROM_carols_triumph }
				}
				complete_national_focus = ROM_carols_triumph
			}
		}
		ai_will_do = {
			factor = 200
		}
	}

	ROM_bessarabia_war_timer = {
		icon = generic_nationalism
		allowed = {
			always = no
		}
		available = {
			AND = {
				NOT = { ROM = { has_war_with = SOV } }
				SOV = { has_war_with = GER }
			}
		}
		days_mission_timeout = 180
		fire_only_once = yes

		cancel_trigger = {
			OR = {
				GER = { has_war_with = SOV }
				NOT = { has_war_with = SOV }
			}
		}

		timeout_effect = {
			custom_effect_tooltip = ROM_victory_soviets
		}

		complete_effect = {
			custom_effect_tooltip = ROM_surrender_soviets
		}
	}


	# 	days_mission_timeout = 140

}
