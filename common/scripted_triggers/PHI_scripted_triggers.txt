# Philippines (PHI) removed from GMFU - content emptied; tag deleted.

# Restored from DEV-MAIN: PHI_SEA (called by DEV zzz_generic)
PHI_SEA = { # Trigger to check if playing as Philippines while having SEA
	original_tag = PHI
	has_dlc = "No Compromise, No Surrender"
}
