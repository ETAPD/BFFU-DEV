# Nordic Alliance mechanic is intentionally fully removed from this mod.
# This stub exists only because vanilla's own debug-only NOR_debug_decisions_category
# (common/decisions/NOR.txt, not overridden here) still references NORDIC_is_nordic
# in a dev/testing decision (NOR_add_to_faction). Kept inert so it satisfies trigger
# validation without reintroducing any real Nordic Alliance behavior.
NORDIC_is_nordic = {
	always = no
}
