on_monthly = {
	effect = {
		if = {
			limit = { original_tag = FRA }
			# Fall of France pre-event
			country_event = { id = france.101 }
			# Capital captured
			country_event = { id = france.16 }
			# Daladier resigns
			country_event = { id = france.31 }
		}
		# ENG: Franco-British Union release
		if = {
			limit = { tag = ENG }
			country_event = { id = france.33 }
		}
	}
}
