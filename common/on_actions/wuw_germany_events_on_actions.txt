on_monthly = {
	effect = {
		if = {
			limit = { original_tag = GER }
			# Lokot Autonomy - Russian volunteers
			country_event = { id = wuw_GER_legions.3 }
			# Russian exiles in Serbia
			country_event = { id = wuw_GER_legions.7 }
			# Battle of Britain
			country_event = { id = wuw_GER_the_battle_of_britain.01 }
			# Horthy's loyalty
			country_event = { id = GER_operation_panzerfaust_events.01 }
			# Todt-Speer-Goering intrigue
			country_event = { id = GER_inner_circle.03 }
		}
		# SOV: Dismantling Lokot Autonomy
		if = {
			limit = { original_tag = SOV }
			country_event = { id = wuw_GER_legions.5 }
		}
		# LUX: Pierre Dupong
		if = {
			limit = { original_tag = LUX }
			country_event = { id = wuw_GER_releasable.2 }
		}
	}
}
