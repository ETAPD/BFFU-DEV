# While a non-aggression pact flag is set, steer both AIs away from attacking each other.

JAP_nap_sov = {
	enable = { original_tag = JAP has_country_flag = JAP_nap_with_SOV }
	abort_when_not_enabled = yes
	ai_strategy = { type = antagonize id = "SOV" value = -1000 }
	ai_strategy = { type = conquer id = "SOV" value = -1000 }
}

SOV_nap_jap = {
	enable = { original_tag = SOV JAP = { has_country_flag = JAP_nap_with_SOV } }
	abort_when_not_enabled = yes
	ai_strategy = { type = antagonize id = "JAP" value = -1000 }
	ai_strategy = { type = conquer id = "JAP" value = -1000 }
}

JAP_nap_ger = {
	enable = { original_tag = JAP has_country_flag = JAP_nap_with_GER }
	abort_when_not_enabled = yes
	ai_strategy = { type = antagonize id = "GER" value = -1000 }
}

JAP_nap_ita = {
	enable = { original_tag = JAP has_country_flag = JAP_nap_with_ITA }
	abort_when_not_enabled = yes
	ai_strategy = { type = antagonize id = "ITA" value = -1000 }
}
