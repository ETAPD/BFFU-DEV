###########################################################
# Nanshin-ron events
###########################################################

add_namespace = jap_guerrilla

# Guerrilla Operation - infiltrators blow up the defences of one enemy stronghold.
# Forts, coastal forts, airfields and anti-air in the chosen state are wrecked.
country_event = {
	id = jap_guerrilla.1
	title = jap_guerrilla.1.t
	desc = jap_guerrilla.1.d
	picture = GFX_report_event_NW_JAP.254
	is_triggered_only = yes

	# Hawaii (state 629)
	option = {
		name = jap_guerrilla.1.a
		trigger = { 629 = { NOT = { is_controlled_by = JAP } } }
		629 = {
			set_building_level = { type = land_fort level = 0 instant_build = yes }
			set_building_level = { type = coastal_bunker level = 0 instant_build = yes }
			set_building_level = { type = anti_air_building level = 0 instant_build = yes }
			set_building_level = { type = air_base level = 0 instant_build = yes }
		}
	}
	# Calcutta (state 431)
	option = {
		name = jap_guerrilla.1.b
		trigger = { 431 = { NOT = { is_controlled_by = JAP } } }
		431 = {
			set_building_level = { type = land_fort level = 0 instant_build = yes }
			set_building_level = { type = coastal_bunker level = 0 instant_build = yes }
			set_building_level = { type = anti_air_building level = 0 instant_build = yes }
			set_building_level = { type = air_base level = 0 instant_build = yes }
		}
	}
	# Batavia / Java (state 335)
	option = {
		name = jap_guerrilla.1.c
		trigger = { 335 = { NOT = { is_controlled_by = JAP } } }
		335 = {
			set_building_level = { type = land_fort level = 0 instant_build = yes }
			set_building_level = { type = coastal_bunker level = 0 instant_build = yes }
			set_building_level = { type = anti_air_building level = 0 instant_build = yes }
			set_building_level = { type = air_base level = 0 instant_build = yes }
		}
	}
}
